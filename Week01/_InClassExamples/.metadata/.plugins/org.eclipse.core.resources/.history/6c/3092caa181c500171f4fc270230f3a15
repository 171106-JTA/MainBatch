import java.util.Comparator;

public class StringComp<String> implements Comparator<String>{

	@Override
	public int compare(String o1, String o2) {
		String min;
		min = a1.length() < a2.length() ? a1 : a2;
		return 0;
	}

}
