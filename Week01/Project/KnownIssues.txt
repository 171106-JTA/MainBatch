List of known issues:

☐ to-fix ☑ fixed

☐ after activated User, that was locked, logs in, User is still "locked" to the Admin
☐ UI prevents locked Users from creating accounts, but someone can still say:
	☐ lockedUser.createAccount()	// and then manipulate that Account
	☐ lockedUser.getAccounts().add(new Account()) // and then manipulate that Account
	